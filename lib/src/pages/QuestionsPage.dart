import 'package:flutter/material.dart';

class QuestionsPage extends StatelessWidget {
  String title = "TEST DE COVID-19 EMI";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("hola questions")),
    );
  }
}
